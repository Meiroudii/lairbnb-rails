<%= link_to listing do %>
  <div class="w-full">
    <%= image_tag(listing.image.first, class: "w-full h-64 rounded-lg overflow-hidden object-cover") if listing.image.any? %>
    <p class="text-lg font-bold"> <%= listing.title %></p>
  </div>
<% end %>
